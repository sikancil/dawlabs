{
  "_comment": {
    "overview": "DAWLabs NestJS TypeScript Configuration - Optimized Compiler Settings for NestJS Applications",
    "description": "Specialized TypeScript configuration for NestJS applications within the DAWLabs monorepo, providing comprehensive decorator support, proper module resolution, and optimized build settings for server-side applications",
    "purpose": "Enables NestJS-specific TypeScript features including decorators, dependency injection metadata, and CommonJS module output for Node.js compatibility",
    "integration": "Extended by NestJS applications and plugins generated by create-dawlabs-package for consistent TypeScript compilation across all NestJS projects"
  },
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "DAWLabs NestJS",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    // NestJS Specific
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "CommonJS",
    "moduleResolution": "node",
    "types": ["node"],

    // NestJS Decorator Support
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,
    "strictPropertyInitialization": false,

    // Build Options
    "outDir": "dist",
    "rootDir": "src",

    // NestJS Compatibility
    "allowSyntheticDefaultImports": true,
    "esModuleInterop": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "coverage", "test/**/*", "**/*.test.ts", "**/*.spec.ts"]
}
